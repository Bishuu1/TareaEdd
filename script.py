import Time
import Faker
import ListaDoble
import abb
import avl
import arbol23
import hashh

def elegir (self,eleccion):
    x = input ("Elija un numero según el tipo de estructura")
    if x == 1:

    if x == 2:

    if x == 3:

    if x == 4:

    if x == 5:

